import { Product } from "./models/Product";

const products: Product[] = [
  { name: "apples", price: 5 },
  { name: "oranges", price: 5 },
  { name: "mangos", price: 11 },
];

export const calcAverageProductPrice = (array: Product[]): number | void => {
  for (let item of array) () => {};
};
console.table(calcAverageProductPrice(products));
